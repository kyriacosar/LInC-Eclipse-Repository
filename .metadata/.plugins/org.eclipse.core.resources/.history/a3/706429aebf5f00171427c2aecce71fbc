'''
Created on Jul 3, 2017

@author: kyriacos
'''

def digitSum(num):
    sumT = 0
    while time > 0:
        rem = time % 10
        time = time / 10
        sumT += rem
    return sumT
def timeCheck(time):
    res = digitSum(time)
    res = digitSum(res)
        
    return timeCheck(sumT)
    
    
if __name__ == "__main__":
    time = 0
    even = 0
    odd = 0
    
    for time in range(0, 2359):
        
        if timeCheck(time):
            even += 1
        else:
            odd += 1
            
        time += 1
        
        if time % 60 == 0:
            time += 100
            
    print("Even time: "+str(even))
    print("Odd time: "+str(odd))
